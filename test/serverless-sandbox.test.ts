// import * as cdk from "aws-cdk-lib/core";
// import { Template } from "aws-cdk-lib/assertions";
// import { DatabaseStack } from "~lib/stacks/database-stack";
// import { ApiStack } from "~lib/stacks/api-stack";

// example test. To run these tests, uncomment this file along with the
// example resource in lib/stacks/database-stack.ts
test("DatabaseStack creates DSQL cluster", () => {
	//   const app = new cdk.App();
	//   const database = new DatabaseStack(app, "TestDatabaseStack", {
	//     migrationsDir: "migrations",
	//   });
	//   const template = Template.fromStack(database);
	//   template.hasResource("AWS::DSQL::Cluster", {});
});
